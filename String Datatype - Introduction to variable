The equivalent of string datatype in other languages is a char array in C. It is used to store a sequence of characters, i.e. text.
String values must be surrounded by double quotes, ie. " ".

Example:

Syntax to declare and initialise a char array
char arr[] = "I love C";
Syntax to display a char array
printf("%s", arr);
Task
Write a program which does the following

Declare two variables 
a & 
b
Assign the values "Code" & "Chef" to 
a and 
b respectively
Display the variables in a single line without any space between them
Note : The format specifier for a string is "%s"

Sol.:-
#include<stdio.h>

int main () {
    char a[] = "Code";
    char b[] = "Chef";
    printf("%s%s",a,b);
    return 0;
}
